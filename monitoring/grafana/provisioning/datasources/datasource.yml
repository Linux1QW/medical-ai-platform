# Grafana 数据源自动配置（容器启动时加载）
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: medical-ai-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
